# 硕士格式规则

1. 培养层次确认为硕士后，路由到 BUAAthesis master 实现；禁止加载本科封面、任务书或本科前置页规则。
2. 标题、作者、学号、学科专业、研究方向、导师、培养单位、日期、密级等字段，只按硕士模板公开 API 和已核验证据填写。
3. 封面、声明、中英文摘要、目录、符号表及其他前置页，以当前 BUAAthesis 硕士模板提供的字段、顺序、分页和样式为准。
4. 模板没有表达某项规范、源证据冲突或无法确定字段归属时，标记 `needs_review` 并阻止最终 `pass`；不得推断默认值。
5. 严禁为追求视觉相似而套用本科页面、字号或元数据槽位。

