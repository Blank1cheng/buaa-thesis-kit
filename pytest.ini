[pytest]
addopts = -m "not legacy_word"
markers =
    latex: active LaTeX-first pipeline tests
    legacy_word: legacy Word renderer and Word pipeline tests
    slow: slow integration or rendering tests
    requires_word_com: requires local Microsoft Word COM automation
    requires_xelatex: requires a local XeLaTeX installation
